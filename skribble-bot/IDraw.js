export default class IDraw {
    constructor(colours, brushes) {
        this.colours = colours;
        this.brushes = brushes;
    }

    async fill(colour){

    }

    async setColour(colour) {

    }

    async setBrushSize(size) {
        
    }

    async moveBrush(path = [[0, 0], [0, 1], [0, 0]]) {

    }
}